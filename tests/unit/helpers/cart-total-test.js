import { cartTotal } from 'ecommerce/helpers/cart-total';
import { module, test } from 'qunit';

module('Unit | Helper | cart total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartTotal([42]);
  assert.ok(result);
});
