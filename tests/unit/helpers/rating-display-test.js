import { ratingDisplay } from 'ecommerce/helpers/rating-display';
import { module, test } from 'qunit';

module('Unit | Helper | rating display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingDisplay([42]);
  assert.ok(result);
});
