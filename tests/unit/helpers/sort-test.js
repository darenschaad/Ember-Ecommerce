import { sort } from 'ecommerce/helpers/sort';
import { module, test } from 'qunit';

module('Unit | Helper | sort');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sort([42]);
  assert.ok(result);
});
